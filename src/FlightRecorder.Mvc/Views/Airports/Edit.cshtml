@model FlightRecorder.Mvc.Models.EditAirportViewModel

@{
    ViewData["Title"] = "Edit Airport";
}

<div class="container-fluid">
    @using (Html.BeginForm())
    {
        <div class="row">
            <div class="col-md">
                <span style="font-size: 1.2rem"><strong>Edit Airport</strong></span>
            </div>
        </div>
        <hr />

        @await Html.PartialAsync("Airport", Model);
        <br />

        <div class="row">
            <div class="col-md">
                <div class="input-group">
                    <button type="submit" class="btn btn-primary">Save</button>
                </div>
            </div>
        </div>
    }
</div>
